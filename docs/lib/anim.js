import{$}from"./dars.min.js";const __observers=new Map();const __scrollHandlers=new Map();let __scrollRAF=null;export const DarsAnimation={_whenReady(fn){if(document.readyState==="loading"){document.addEventListener("DOMContentLoaded",fn,{once:true});}else{fn();}},_setStylesWithoutTransition(el,styles){if(!el||!styles)return;try{const oldTrans=el.style.getPropertyValue('transition');const oldPri=el.style.getPropertyPriority('transition');el.style.setProperty('transition','none','important');for(const k in styles){if(k!=='offset'&&k!=='easing'){el.style[k]=styles[k];}}
void el.offsetHeight;if(oldTrans)el.style.setProperty('transition',oldTrans,oldPri);else el.style.removeProperty('transition');}catch(e){}},observe(id,opts={}){const _run=()=>{try{const el=$(id);if(!el)return;if(__observers.has(id)){__observers.get(id).disconnect();__observers.delete(id);}
const threshold=opts.threshold??0.1;const rootMargin=opts.rootMargin||"0px";const once=opts.once!==false;const observer=new IntersectionObserver((entries)=>{for(const entry of entries){if(entry.isIntersecting){if(opts.enterClass)el.classList.add(opts.enterClass);if(opts.leaveClass)el.classList.remove(opts.leaveClass);if(typeof opts.onEnter==="function")opts.onEnter(el,entry);if(once){observer.unobserve(el);__observers.delete(id);}}else{if(opts.leaveClass)el.classList.add(opts.leaveClass);if(opts.enterClass)el.classList.remove(opts.enterClass);if(typeof opts.onLeave==="function")opts.onLeave(el,entry);}}},{threshold,rootMargin});observer.observe(el);__observers.set(id,observer);}catch(e){console.error("[DarsAnimation:observe]",e);}};this._whenReady(_run);},unobserve(id){if(__observers.has(id)){__observers.get(id).disconnect();__observers.delete(id);}},animate(id,keyframes,options={}){return new Promise((resolve,reject)=>{try{const el=$(id);if(!el){resolve();return;}
const opts={duration:options.duration??300,easing:options.easing||"ease",fill:options.fill||"forwards",iterations:options.iterations??1,delay:options.delay??0,direction:options.direction||"normal",};if(options.iterations==="infinite")opts.iterations=Infinity;const anim=el.animate(keyframes,opts);anim.onfinish=()=>{try{if(opts.fill==="forwards"&&keyframes.length>0){const finalFrame=keyframes[keyframes.length-1];DarsAnimation._setStylesWithoutTransition(el,finalFrame);anim.cancel();}}catch(_){}
resolve(anim);};anim.oncancel=()=>resolve(anim);}catch(e){console.error("[DarsAnimation:animate]",e);resolve();}});},animateOnView(id,keyframes,animOpts={},viewOpts={}){const self=this;this._whenReady(()=>{try{const el=$(id);if(el&&keyframes.length>0){self._setStylesWithoutTransition(el,keyframes[0]);}}catch(_){}
self.observe(id,{threshold:viewOpts.threshold??0.1,rootMargin:viewOpts.rootMargin||"0px",once:viewOpts.once!==false,onEnter(el){self.animate(id,keyframes,animOpts);},onLeave:viewOpts.onLeave||null,});});},stagger(ids,keyframes,options={}){const staggerDelay=options.staggerDelay??100;const promises=ids.map((id,i)=>{const opts=Object.assign({},options,{delay:(options.delay??0)+i*staggerDelay,});delete opts.staggerDelay;return this.animate(id,keyframes,opts);});return Promise.all(promises);},staggerOnView(ids,keyframes,animOpts={},viewOpts={}){if(!ids.length)return;const self=this;this._whenReady(()=>{try{if(keyframes.length>0){ids.forEach((id)=>{const el=$(id);if(el)self._setStylesWithoutTransition(el,keyframes[0]);});}}catch(_){}
self.observe(ids[0],{threshold:viewOpts.threshold??0.1,rootMargin:viewOpts.rootMargin||"0px 0px -50px 0px",once:viewOpts.once!==false,onEnter(){self.stagger(ids,keyframes,animOpts);},});});},async timeline(steps){for(const step of steps){if(step.delay)await new Promise((r)=>setTimeout(r,step.delay));await this.animate(step.id,step.keyframes,step.options||{});}},scrollProgress(id,opts={}){const config={property:opts.property||"opacity",from:opts.from??0,to:opts.to??1,unit:opts.unit||"",start:opts.start??0,end:opts.end??1,};__scrollHandlers.set(id,config);if(!__scrollRAF){const tick=()=>{const scrollTop=window.scrollY||document.documentElement.scrollTop;const docHeight=Math.max(document.documentElement.scrollHeight-window.innerHeight,1,);const progress=Math.min(Math.max(scrollTop/docHeight,0),1);for(const[eid,cfg]of __scrollHandlers){try{const el=$(eid);if(!el)continue;const range=cfg.end-cfg.start;const local=range>0?Math.min(Math.max((progress-cfg.start)/range,0),1):0;const val=cfg.from+(cfg.to-cfg.from)*local;el.style[cfg.property]=val+cfg.unit;}catch(_){}}
__scrollRAF=requestAnimationFrame(tick);};__scrollRAF=requestAnimationFrame(tick);}},removeScrollProgress(id){__scrollHandlers.delete(id);if(__scrollHandlers.size===0&&__scrollRAF){cancelAnimationFrame(__scrollRAF);__scrollRAF=null;}},classOnView(id,className,opts={}){this.observe(id,{threshold:opts.threshold??0.1,rootMargin:opts.rootMargin||"0px",once:opts.once!==false,enterClass:className,leaveClass:opts.leaveClass||null,});},runOnView(id,code,opts={}){this.observe(id,{threshold:opts.threshold??0.1,rootMargin:opts.rootMargin||"0px",once:opts.once!==false,onEnter:code,});},destroy(){for(const[,obs]of __observers)obs.disconnect();__observers.clear();__scrollHandlers.clear();if(__scrollRAF){cancelAnimationFrame(__scrollRAF);__scrollRAF=null;}},};