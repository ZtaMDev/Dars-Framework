document.addEventListener('DOMContentLoaded',function initNav(){const navRight=document.getElementById('navbar-right');const burger=document.getElementById('navbar-hamburger');if(!burger||!navRight)return;let overlay=document.getElementById('nav-overlay');if(!overlay){overlay=document.createElement('div');overlay.id='nav-overlay';overlay.style.position='fixed';overlay.style.top='0';overlay.style.left='0';overlay.style.width='100%';overlay.style.height='100%';overlay.style.background='rgba(0,0,0,0.35)';overlay.style.zIndex='1999';overlay.style.display='none';document.body.appendChild(overlay);}
function openMenu(){navRight.classList.add('open');overlay.style.display='block';}
function closeMenu(){navRight.classList.remove('open');overlay.style.display='none';}
burger.addEventListener('click',function(e){e.stopPropagation();if(navRight.classList.contains('open'))closeMenu();else openMenu();});overlay.addEventListener('click',function(){closeMenu();});const links=navRight.querySelectorAll('.nav-link');links.forEach(link=>link.addEventListener('click',closeMenu));document.addEventListener('click',function(ev){const target=ev.target;if(!navRight.contains(target)&&target!==burger){if(navRight.classList.contains('open'))closeMenu();}});});window.addEventListener('load',()=>{const overlay=document.getElementById('page-overlay');if(overlay){overlay.style.opacity='0';setTimeout(()=>overlay.remove(),1600);}});document.addEventListener('DOMContentLoaded',()=>{const markdownBlocks=document.querySelectorAll('.markdown-docs');markdownBlocks.forEach(block=>{block.classList.add('lazy-hidden');});const observerOptions={root:null,rootMargin:'300px',threshold:0.1};const observer=new IntersectionObserver((entries)=>{entries.forEach(entry=>{if(entry.isIntersecting){entry.target.classList.add('lazy-visible');entry.target.classList.remove('lazy-hidden');}else{entry.target.classList.remove('lazy-visible');entry.target.classList.add('lazy-hidden');}});},observerOptions);markdownBlocks.forEach(block=>observer.observe(block));});function isMobileDevice(){return(typeof window.orientation!=="undefined")||(navigator.userAgent.indexOf('IEMobile')!==-1)||(navigator.userAgent.indexOf('Android')!==-1)||(navigator.userAgent.indexOf('iPhone')!==-1)||(navigator.userAgent.indexOf('iPad')!==-1)||(navigator.userAgent.indexOf('iPod')!==-1)||(window.innerWidth<=850);}
function handlePlaygroundLink(){var playgroundLink=document.getElementById('linkPlayground');if(!playgroundLink)return;playgroundLink.style.display="";}
function initMobileDetection(){handlePlaygroundLink();window.addEventListener('resize',handlePlaygroundLink);}
if(document.readyState==='loading'){document.addEventListener('DOMContentLoaded',initMobileDetection);}else{initMobileDetection();}
console.log('Dars-framework version: 1.1.8 running...')